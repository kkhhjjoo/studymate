info:
  name: 북마크 추가
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/bookmarks/product"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
