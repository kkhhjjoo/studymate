info:
  name: 회원목록 조회
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/users/"
  headers:
    - name: client-id
      value: "{{client-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
