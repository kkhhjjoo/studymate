info:
  name: db초기화
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/db/init"
  headers:
    - name: client-id
      value: "{{client-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
