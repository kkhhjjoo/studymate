info:
  name: 로그인
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/users/login"
  headers:
    - name: client-id
      value: "{{client-id}}"
    - name: Authorization
      value: bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "email": "khjoo1203@naver.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
